package com.summer.iam.application.service;

/**
 * 授权服务 (Authorization Service): 负责管理权限的分配和撤销。
 * assignPermissionToRole(role, permission): 将权限赋予给角色。
 * revokePermissionFromRole(role, permission): 从角色中移除权限。
 * assignRoleToUser(user, role): 将角色赋予给用户。
 * removeRoleFromUser(user, role): 从用户身上移除角色。
 */
public class AuthorizationService {
}
